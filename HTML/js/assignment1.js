//Create a function named remainder which take one argument y and prints the remainder of y if divided by 3.

function remainder(y){
    let rem = y % 3;
    console.log("Remainder of", y ," is divided by 3 is",rem);
}